This is a programming assignment for the Square interview

Write a library that sorts and prints the words in a given twitter
user's last 1000 tweets sorted by frequency of use.

Before running this script, make sure the jquery library is installed
use command:
npm install jquery